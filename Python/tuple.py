# Create a Tuple
log_entry= ("192.168.1.1", 80)

# Accessing Tuple Elements
print(log_entry[0]) # Expected output

#Tuple Immutability(Comment Out)
#log_entry[0] = "10.0.0.0"

#Gert the Length of a Tuple
print("Tuple length:", len(log_entry))